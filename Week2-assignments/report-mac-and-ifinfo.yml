---
- name: Process and filter information from collected MAC-address table and interface status information

  vars:
  - results: results

  tasks:
  - name: Create directory for results
    file: path={{results}} state=directory
    run_once: true

  # Task which opens Interface information and picks up status info and writes to file

  # Task which opens MAC-address table, picks up MAC-address and interface and updates result file line by adding list of MAC-addresses for a particula interface